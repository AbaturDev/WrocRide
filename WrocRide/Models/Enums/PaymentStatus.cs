namespace WrocRide.Models.Enums
{
    public enum PaymentStatus
    {
        Pending = 1,            // Oczekujące
        Paid = 2,               // Zapłacone
        Canceled = 3            // Anulowane
    }
}
